# The four product repos are their own git repos with their own lifetimes (DECISIONS.md D6).
auros-base/
auros-recipes/
auros-installer/
auros-web/

node_modules/
.DS_Store
*.log
.wrangler/
dist/

# Moved to the private repo aarohkandy/auros_private (DECISIONS.md D36). Ignored here so that no
# git add -A can sweep a copy back into a public repository — which is how it got here the first time.
advertising/
