# The four product repos are their own git repos with their own lifetimes (DECISIONS.md D6).
auros-base/
auros-recipes/
auros-installer/
auros-web/

node_modules/
.DS_Store
*.log
.wrangler/
dist/

# HELD pending the owner: a go-to-market strategy may not belong in a public repo.
# It was committed once without review (swept in by git add -A). See the session log.
advertising/
